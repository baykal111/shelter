# Сайт приюта для животных "Верные друзья"

Современный веб-сайт для приюта животных с полным функционалом: просмотр животных, бронирование встреч, система аккаунтов, поддержка пользователей и многое другое.

## Возможности

### Для пользователей:
- 🐕 Просмотр доступных животных с фильтрацией
- 📅 Бронирование встреч с животными
- 👤 Регистрация и личный кабинет
- 💬 Связь с поддержкой
- 💰 Возможность делать пожертвования
- 🤝 Информация о волонтерстве

### Для администраторов:
- 📊 Полная админ-панель Django
- 🐾 Управление животными
- 📝 Управление бронированиями
- 💌 Обработка обращений
- 👥 Управление пользователями
- 📈 Статистика и отчеты

## Структура проекта

```
shelter_project/
│
├── shelter/                    # Основное приложение
│   ├── static/                # Статические файлы
│   │   ├── css/
│   │   │   └── style.css     # Стили сайта
│   │   └── js/
│   │       └── script.js     # JavaScript
│   │
│   ├── templates/             # HTML шаблоны
│   │   └── shelter/
│   │       └── index.html    # Главная страница
│   │
│   ├── models.py             # Модели базы данных
│   ├── views.py              # Представления
│   ├── urls.py               # URL маршруты
│   ├── forms.py              # Формы
│   └── admin.py              # Настройка админки
│
├── media/                     # Загружаемые файлы
│   ├── animals/              # Фото животных
│   └── avatars/              # Аватары пользователей
│
├── requirements.txt          # Зависимости Python
└── manage.py                 # Управление Django
```

## Установка и запуск

### 1. Клонируйте репозиторий или создайте проект

```bash
django-admin startproject shelter_project
cd shelter_project
python manage.py startapp shelter
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте settings.py

Добавьте в `INSTALLED_APPS`:

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'shelter',  # Ваше приложение
    'crispy_forms',
    'crispy_bootstrap5',
]

# Настройка для crispy forms
CRISPY_ALLOWED_TEMPLATE_PACKS = "bootstrap5"
CRISPY_TEMPLATE_PACK = "bootstrap5"

# Настройка пользовательской модели
AUTH_USER_MODEL = 'shelter.CustomUser'

# Настройка медиа файлов
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# Настройка статических файлов
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_DIRS = [BASE_DIR / 'shelter' / 'static']
```

### 4. Настройте URLs в главном urls.py проекта

```python
from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('shelter.urls')),
]

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
    urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
```

### 5. Создайте базу данных и миграции

```bash
python manage.py makemigrations
python manage.py migrate
```

### 6. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

### 7. Соберите статические файлы

```bash
python manage.py collectstatic --noinput
```

### 8. Запустите сервер разработки

```bash
python manage.py runserver
```

Сайт будет доступен по адресу: `http://127.0.0.1:8000/`
Админ-панель: `http://127.0.0.1:8000/admin/`

## Структура файлов

### HTML (index.html)
- Главная страница с героем
- Форма поиска и фильтрации
- Карточки животных
- Модальные окна для входа, регистрации, бронирования
- Футер с контактами

### CSS (style.css)
- Современный адаптивный дизайн
- Анимации и переходы
- Система цветов (CSS переменные)
- Мобильная версия
- Стили для модальных окон

### JavaScript (script.js)
- Управление модальными окнами
- Валидация форм
- Форматирование телефонов
- AJAX запросы (опционально)
- Анимации при скролле

### Models (models.py)
- **CustomUser**: Расширенная модель пользователя
- **Animal**: Модель животного
- **Reservation**: Бронирование встреч
- **SupportRequest**: Обращения в поддержку
- **Adoption**: Усыновления
- **Donation**: Пожертвования

### Views (views.py)
- Главная страница
- Список животных с фильтрацией
- Детальная страница животного
- Регистрация/вход/выход
- Профиль пользователя
- Создание бронирования
- Обращения в поддержку
- API endpoints

## Модели базы данных

### CustomUser
- Базовая информация пользователя
- Телефон, аватар, адрес
- Дата рождения
- Статус верификации

### Animal
- Имя, тип (собака/кошка/другое)
- Порода, возраст, пол, размер
- Описание, состояние здоровья
- Фото
- Статус (доступен/забронирован/усыновлен)
- Вакцинация, стерилизация

### Reservation
- Связь с животным и пользователем
- Контактная информация
- Дата посещения
- Комментарий
- Статус (ожидает/подтверждено/завершено/отменено)

### SupportRequest
- Связь с пользователем
- Тема обращения
- Сообщение и ответ
- Статус обработки

## Дополнительные функции

### Фильтрация животных
Пользователи могут фильтровать животных по:
- Типу (собака, кошка, другое)
- Возрасту (молодой, взрослый, пожилой)
- Полу
- Размеру

### Система бронирования
- Выбор животного
- Заполнение контактной информации
- Выбор даты посещения
- Отправка заявки администратору
- Уведомления на email (опционально)

### Личный кабинет
- Просмотр истории бронирований
- Управление профилем
- История пожертвований
- История усыновлений

## Безопасность

- CSRF защита для всех форм
- Валидация данных на клиенте и сервере
- Хеширование паролей
- Ограничение прав доступа
- SQL injection защита (Django ORM)

## Развертывание в продакшн

### Используйте переменные окружения

Создайте файл `.env`:

```env
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-domain.com
DATABASE_URL=postgresql://user:password@localhost/dbname
```

### Настройте базу данных PostgreSQL

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'shelter_db',
        'USER': 'shelter_user',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

### Используйте Gunicorn

```bash
gunicorn shelter_project.wsgi:application --bind 0.0.0.0:8000
```

### Настройте Nginx как reverse proxy

### Используйте SSL сертификат (Let's Encrypt)

## Поддержка

Для вопросов и предложений создавайте issues в репозитории или связывайтесь с администратором.

## Лицензия

MIT License

## Автор

Разработано для приюта "Верные друзья"

---

**Примечание**: Это базовая версия. Вы можете расширить функционал добавив:
- Email уведомления
- SMS уведомления
- Онлайн-платежи
- Блог
- Галерею
- Отзывы
- Интеграцию с соц. сетями
- Многоязычность
- И многое другое!
